# CashLait Bot - Бот для заработка на заданиях

Telegram-бот для заработка на выполнении заданий (подписки на каналы) с автоматическим выводом криптовалюты через Crypto Pay API.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install pyTelegramBotAPI requests
```

### 2. Настройка переменных окружения

Создайте файл `.env` или установите переменные:

```bash
# Обязательные
export CASHLAIT_BOT_TOKEN="ваш_токен_от_BotFather"
export ADMIN_IDS="ваш_telegram_id"

# Опциональные
export CONSTRUCTOR_BOT_USERNAME="имя_бота_конструктора"
export CASHLAIT_DB="путь/к/базе.db"
export CASHLAIT_LOG="путь/к/лог.log"
```

### 3. Запуск бота

```bash
python "cashlait_bot (6).py"
```

## ⚙️ Настройка через админ-панель

После запуска отправьте боту команду `/admin` и настройте:

### 1. Основные настройки
- Символ валюты (по умолчанию: USDT)
- Награда за задание
- Минимальная сумма вывода

### 2. Flyer API (для автоматических заданий)
- API ключ от @FlyerServiceBot
- Лимит выдачи заданий

### 3. **Резерв - Crypto Pay API** ⭐
1. Получите токен в @CryptoBot:
   - Откройте @CryptoBot
   - Перейдите в Crypto Pay
   - Нажмите "Создать приложение"
   - Скопируйте токен

2. В боте: `/admin` → `💸 Резерв` → `⚙️ Настройки`
   - Вставьте Crypto Pay токен
   - Укажите актив для выплат (USDT, TON и т.д.)

3. Пополните резерв:
   - `💸 Резерв` → `➕ Пополнить`

## 📝 Основной функционал

### Для пользователей:
- 📱 **Личный кабинет** - баланс и статистика
- 📝 **Задания** - выполнение заданий за вознаграждение
- 💰 **Вывод средств** - автоматический вывод через Crypto Pay
- 👥 **Рефералы** - 2 уровня (15% + 5%)
- 📣 **Продвижение** - реферальная ссылка

### Для администраторов:
- ⚙️ **Настройки бота**
- 💸 **Управление резервом**
- 📢 **Рассылки**
- 📝 **Управление заданиями**
- 📊 **Статистика**

## 🔧 Настройка ссылки на конструктор

Ссылка "🤖 Хочу такого же бота" настраивается **ТОЛЬКО В КОДЕ**:

### Способ 1: Через переменную окружения (рекомендуется)
```bash
export CONSTRUCTOR_BOT_USERNAME="ВашБот_bot"
```

### Способ 2: В файле (строка 36)
```python
CONSTRUCTOR_BOT_USERNAME = os.getenv("CONSTRUCTOR_BOT_USERNAME", "ВашБот_bot")
```

## 🔐 Безопасность

⚠️ **ВАЖНО**: Никогда не публикуйте токены и API ключи в публичных репозиториях!

Используйте файл `.env`:
```bash
# .env
CASHLAIT_BOT_TOKEN=ваш_токен
ADMIN_IDS=ваш_id
CONSTRUCTOR_BOT_USERNAME=ваш_бот
```

И добавьте в `.gitignore`:
```
.env
cashlait.db
cashlait_bot.log
__pycache__/
*.pyc
```

## 📊 Структура базы данных

SQLite база данных включает:
- `users` - пользователи и их балансы
- `task_logs` - история выполнения заданий
- `pending_tasks` - активные задания
- `custom_tasks` - кастомные задания от админа
- `settings` - настройки бота

## 🤝 Поддерживаемые API

1. **Flyer API** (`api.flyerservice.io`) - задания по подпискам
2. **Crypto Pay API** (`pay.crypt.bot`) - выплаты криптовалюты

Поддерживаемые активы: USDT, TON, BTC, ETH, LTC, BNB, TRX, USDC

## 📝 Лицензия

Проект для личного использования.

## 🆘 Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота
2. Настройки Crypto Pay API
3. Логи в файле `cashlait_bot.log`

---

**Версия**: 1.0  
**Последнее обновление**: 18 ноября 2025
