# Изменения в CashLait Bot

## Дата: 18 ноября 2025

### ✅ Выполненные изменения:

#### 1. **Валюта по умолчанию - USDT** ✓
- ✅ Уже было настроено по умолчанию
- `currency_symbol`: "USDT"
- `crypto_pay_asset`: "USDT"
- `reserve_invoice_asset`: "USDT"

#### 2. **Убрана настройка ссылки "Хочу такого же бота" из админки** ✓
- ❌ Удалено из `INFO_LINK_FIELDS` (строка 105)
- ❌ Удалено из `DEFAULT_SETTINGS` (строка 77)
- ✅ Теперь ссылка берется только из константы `CONSTRUCTOR_BOT_LINK`
- ✅ Настраивается в начале файла через переменную окружения:
  ```python
  CONSTRUCTOR_BOT_USERNAME = os.getenv("CONSTRUCTOR_BOT_USERNAME", "MinxoCreate_bot").strip("@ ")
  CONSTRUCTOR_BOT_LINK = f"https://t.me/{CONSTRUCTOR_BOT_USERNAME}"
  ```

#### 3. **Исправлена кнопка "Резерв"** ✓
Функция `show_reserve_panel()` теперь работает правильно:

##### **Когда Crypto Pay API НЕ настроен:**
```
💸 Резерв Crypto Pay

⚠️ Crypto Pay API не настроен

Для работы с резервом необходимо:
1. Получить токен в @CryptoBot → Crypto Pay → Создать приложение
2. Указать токен в настройках ниже
3. Настроить актив для выплат (USDT, TON, и т.д.)

После настройки здесь будет отображаться баланс резерва.

[⚙️ Настройки] [⬅️ Назад]
```

##### **Когда токен настроен неправильно:**
```
💸 Резерв Crypto Pay

⚠️ Ошибка подключения к Crypto Pay API:
[текст ошибки]

Проверьте правильность токена в настройках.

[⚙️ Настройки] [⬅️ Назад]
```

##### **Когда всё работает:**
```
💸 Резерв Crypto Pay

USDT: доступно 100.500 / удержано 10.200
TON: доступно 50.000 / удержано 5.000

[⚙️ Настройки]
[➕ Пополнить] [➖ Вывести]
[⬅️ Назад]
```

---

## 📝 Изменённые файлы:

- `Cash/cashlait_bot (6).py`
  - Строки 101-106: Убрана `info_copy_bot_url` из `INFO_LINK_FIELDS`
  - Строка 77: Убрана из `DEFAULT_SETTINGS`
  - Строки 2091-2093: Кнопка теперь использует константу `CONSTRUCTOR_BOT_LINK`
  - Строки 3548-3600: Полностью переписана функция `show_reserve_panel()`

---

## 🎯 Как настроить ссылку на конструктор:

### Способ 1: Через переменную окружения (рекомендуется)
```bash
export CONSTRUCTOR_BOT_USERNAME="ВашБот_bot"
```

### Способ 2: Прямо в коде (строка 36)
```python
CONSTRUCTOR_BOT_USERNAME = os.getenv("CONSTRUCTOR_BOT_USERNAME", "MinxoCreate_bot").strip("@ ")
```
Замените `"MinxoCreate_bot"` на имя вашего бота-конструктора.

---

## ✅ Результат:

1. ✅ Валюта везде по умолчанию USDT
2. ✅ Ссылка на конструктор настраивается только в коде (константа)
3. ✅ Кнопка "Резерв" работает всегда и показывает понятные сообщения
4. ✅ Админ видит инструкции, если Crypto Pay не настроен
5. ✅ Админ видит ошибки, если токен неправильный
6. ✅ Админ видит баланс и кнопки, когда всё работает

